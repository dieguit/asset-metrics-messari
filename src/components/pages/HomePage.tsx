import React from 'react';
import type { NextPage } from 'next';
import { Typography } from '@mui/material';

const Home: NextPage = () => {
  return <Typography>Hi! Please select an item in the left bar.</Typography>;
};

export default Home;
